# Serve pra quando vc precisa ir de um numero ao outro

(1...5).each { |item| puts item  } # vai até o penultimo numero

# quando vc precisar ir de um numero ate exatamente o outro, usar os dois pontos

puts "---------------"

(1..5).each { |item| puts item }